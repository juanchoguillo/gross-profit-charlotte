# Python
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Client data — never commit MC Granite's exports / financials
exmaple_files/
*.csv
*.xlsx
*.xlsb
config.json
order_overrides.json

# Allow the (non-sensitive) mapping template back in
!shop_manager_mapping_template.xlsx
